## exportPattern("^[[:alpha:]]+")
export(
    ## TeXunits,
    char2num,
    expstr,
    rmrp,
    rmspace,
    strexp,
    toHTML,
    toText,
    trim,
    valign
)
S3method(toText, default)
S3method(print, text)
