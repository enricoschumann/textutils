## exportPattern("^[[:alpha:]]+")
export(
    HTMLdecode,
    HTMLencode,
    TeXencode,
    TeXunits,
    btable,
    dctable,
    fill_in,
    here,
    latexrule,
    rmrp,
    spaces,
    strexp,
    title_case,
    toHTML,
    toText,
    trim,
    valign
)
S3method(print, text)
S3method(toHTML, data.frame)
S3method(toHTML, text)
S3method(toLatex, data.frame)
S3method(toText, default)

importFrom("utils",
           "capture.output",
           "read.table", 
           "toLatex",
           "type.convert")
