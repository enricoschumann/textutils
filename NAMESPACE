## exportPattern("^[[:alpha:]]+")
export(
    ## TeXunits,
    btable,
    char2num,
    latexrule,
    rmrp,
    rmspace,
    strexp,
    toHTML,
    toText,
    trim,
    valign
)
S3method(print, text)
S3method(toHTML, text)
S3method(toText, default)
